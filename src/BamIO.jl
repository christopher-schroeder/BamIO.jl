module BamIO
export refid, position, l_read_name, mq, n_cigar_op, flag, l_seq, next_refid, next_position, tlen, read_name, cigar, seq, qualities, eof, nextread
include("bam.jl")

# package code goes here

end # module
